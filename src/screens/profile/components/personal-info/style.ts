import styled from '@emotion/styled';

export const Wrapper = styled('div')`
  button {
    width: 100%;
    height: 48px;
    border-radius: 8px;
    font-size: 16px;
    font-family: var(--font-readex);
    font-weight: 400;
  }
`;
